server:
  port: 9000
  datasource:
    url: jdbc:mysql://localhost:3306/user-registration
    username: root
    password: EasyPass
    driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      show-sql: true
      hibernate:
        ddl-auto: create

